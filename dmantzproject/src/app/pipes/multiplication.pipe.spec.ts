import { MultiplicationPipe } from './multiplication.pipe';

describe('MultiplicationPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplicationPipe();
    expect(pipe).toBeTruthy();
  });
});
